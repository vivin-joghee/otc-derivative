python-stdnum>=1.20
pycountry>=24.0
flask>=3.0
plotly>=5.20

# Optional — only required to rebuild the report artefacts via the
# scripts/build_report_*.py helpers
markdown>=3.5
xhtml2pdf>=0.2
pypandoc-binary>=1.13
